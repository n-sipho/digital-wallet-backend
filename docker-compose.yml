# ==============================================================================
# Docker Compose Configuration (docker-compose.yml)
#
# Best Practice:
# - Orchestrate local development dependencies (API, Database, Redis, etc.).
# - Use environment variables loaded from `.env`.
# - Use named volumes for persistent data storage across container restarts.
# ==============================================================================

# services:
#   api:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     ports:
#       - "5000:5000"
#     environment:
#       - NODE_ENV=development
#       - PORT=5000
#       - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/rewards_db
#     volumes:
#       - ./src:/app/src
#     depends_on:
#       - postgres
#
#   postgres:
#     image: postgres:16-alpine
#     environment:
#       - POSTGRES_USER=postgres
#       - POSTGRES_PASSWORD=postgres
#       - POSTGRES_DB=rewards_db
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#
# volumes:
#   pgdata:
