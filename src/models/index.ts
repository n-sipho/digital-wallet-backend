/**
 * @file index.ts (models)
 * @description Data model and entity definitions (ORM/ODM schemas or table definitions).
 *
 * Best Practices:
 * 1. Define database entity shapes, column mappings, constraints, indices, and relationships.
 * 2. Keep database schema definitions decoupled from HTTP request/response DTOs.
 * 3. Export model schemas or entity classes for use across repositories.
 */

export {};
