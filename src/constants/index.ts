/**
 * @file index.ts (constants)
 * @description Centralized application constants, enums, and static configuration values.
 *
 * Best Practices:
 * 1. Define HTTP status codes, error codes, role identifiers, and system limits here.
 * 2. Use `as const` assertions or TypeScript `enum`s to enforce immutability and precise typing.
 * 3. Avoid magic strings and numbers in business logic; reference these constants instead.
 */

export {};
