/**
 * @file index.ts (services)
 * @description Core business logic layer.
 *
 * Best Practices:
 * 1. Implement all business rules, reward calculations, point deductions, and workflow orchestrations here.
 * 2. Keep services completely independent of HTTP concepts (do NOT accept `req` or `res` in service methods).
 * 3. Interact with data persistence strictly via repositories.
 * 4. Throw custom domain/operational errors when invariants or rules fail.
 * 5. Unit test this layer extensively with mocked repositories and external clients.
 */

export {};
